import React from "react";
import { ActionLoadingElement } from "@styles/components/atom/loading.element";

export const ActionLoading = () => {
  return <ActionLoadingElement size={20} loading={true} />;
};
